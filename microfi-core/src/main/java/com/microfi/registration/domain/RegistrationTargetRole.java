package com.microfi.registration.domain;

/** Which operational account a {@link RegistrationApplication} provisions once approved. */
public enum RegistrationTargetRole {
    AGENT,
    BRANCH_MANAGER,
    BRANCH_CASHIER
}
